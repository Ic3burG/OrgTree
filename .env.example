# Frontend Environment Variables

# API Base URL (only needed if frontend/backend deployed separately)
# Leave empty for same-origin deployment (when Express serves the frontend)
# Example for separate deployment: https://api.yourapp.com
VITE_API_BASE_URL=""
